(() => { const form = document.querySelector('.path-category-form'); if (!form) return; const get = n => form.querySelector(`[name="${n}"]`); const slug = v => String(v || '').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''); const update = () => { const en = form.querySelector('.path-category-tabs .is-active')?.dataset.pcTab === 'pc-en', p = en ? 'English' : 'French'; form.querySelector('[data-pc-preview-title]').textContent = get(`${p}.Title`)?.value || 'Titre de la catégorie'; form.querySelector('[data-pc-preview-summary]').textContent = get(`${p}.Summary`)?.value || 'Aucun résumé pour cette langue.'; form.querySelector('[data-pc-preview-status]').textContent = `${en ? 'EN' : 'FR'} : ${get(`${p}.PublicationStatus`)?.value || ''}`; form.querySelector('[data-pc-preview-slug]').textContent = `/${get(`${p}.Slug`)?.value || 'slug'}`; }; form.querySelectorAll('[data-pc-from]').forEach(b => b.addEventListener('click', () => { const t = get(b.dataset.pcTarget); if (t && !t.value) t.value = slug(get(b.dataset.pcFrom)?.value); })); form.querySelectorAll('.path-category-tabs button').forEach(tab => tab.addEventListener('click', () => { form.querySelectorAll('.path-category-tabs button').forEach(x => { x.classList.toggle('is-active', x === tab); x.setAttribute('aria-selected', x === tab); }); form.querySelectorAll('.path-category-tab-panel').forEach(x => x.hidden = x.id !== tab.dataset.pcTab); update(); })); form.querySelectorAll('input,textarea,select').forEach(x => x.addEventListener('input', update)); update(); })();